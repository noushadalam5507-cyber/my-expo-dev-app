{
  "entities": {
    "users": {
      "title": "User Profiles",
      "description": "User profile identities with Google Auth bindings and handle information.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "avatar": { "type": "string" },
        "username": { "type": "string" },
        "isVerified": { "type": "boolean" },
        "status": { "type": "string" },
        "customBio": { "type": "string" },
        "joinedDate": { "type": "string" },
        "updatedAt": { "type": "string" }
      },
      "required": ["id", "name", "email", "username"]
    },
    "usernames": {
      "title": "Username Registry",
      "description": "Unique handles and verification statuses (including @naushad VIP claim).",
      "type": "object",
      "properties": {
        "username": { "type": "string" },
        "ownerUid": { "type": "string" },
        "ownerEmail": { "type": "string" },
        "ownerName": { "type": "string" },
        "claimedAt": { "type": "string" },
        "isVIP": { "type": "boolean" }
      },
      "required": ["username", "ownerEmail"]
    },
    "rooms": {
      "title": "Calling Rooms",
      "description": "Active WebRTC purple neon audio and video studio rooms.",
      "type": "object",
      "properties": {
        "roomId": { "type": "string" },
        "hostUsername": { "type": "string" },
        "hostName": { "type": "string" },
        "hostAvatar": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["roomId", "hostUsername"]
    },
    "chat_blocks": {
      "title": "Chat Blocks and Moderation",
      "description": "Real-time block registry for chat moderation, self-unblock coin transactions, and AdMob rewarded flows.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "threadId": { "type": "string" },
        "blockerId": { "type": "string" },
        "blockerUsername": { "type": "string" },
        "blockedId": { "type": "string" },
        "blockedUsername": { "type": "string" },
        "blockedAt": { "type": "string" },
        "isBlocked": { "type": "boolean" },
        "unblockedWithCoins": { "type": "boolean" }
      },
      "required": ["threadId", "blockerId", "blockedId", "isBlocked"]
    },
    "reels": {
      "title": "Viral Reels",
      "description": "User recorded or uploaded short-form video reels synced across Firebase Cloud.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "authorUsername": { "type": "string" },
        "authorAvatar": { "type": "string" },
        "videoUrl": { "type": "string" },
        "caption": { "type": "string" },
        "audioTrackTitle": { "type": "string" },
        "audioTrackArtist": { "type": "string" },
        "category": { "type": "string" },
        "likesCount": { "type": "number" },
        "viewsCount": { "type": "number" },
        "createdAt": { "type": "string" }
      },
      "required": ["id", "authorUsername", "videoUrl"]
    }
  },
  "firestoreRules": "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    match /users/{userId} {\n      allow read, write: if true;\n    }\n    match /accounts/{accountId} {\n      allow read, write: if true;\n    }\n    match /wallet_transactions/{txId} {\n      allow read, write: if true;\n    }\n    match /usernames/{username} {\n      allow read, write: if true;\n    }\n    match /chat_blocks/{blockId} {\n      allow read, write: if true;\n    }\n    match /rooms/{roomId} {\n      allow read, write: if true;\n      match /messages/{messageId} {\n        allow read, write: if true;\n      }\n    }\n    match /posts/{postId} {\n      allow read, write: if true;\n    }\n  }\n}"
}
